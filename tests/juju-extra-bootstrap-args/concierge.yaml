juju:
  channel: 4.0/stable
  model-defaults:
    test-mode: "true"
  extra-bootstrap-args: --config max-charm-state-size=0

providers:
  lxd:
    enable: true
    bootstrap: true
    channel: latest/stable
